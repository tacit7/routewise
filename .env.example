NODE_ENV=development

# Database URL for Docker (connects to postgres service)
DATABASE_URL=postgresql://postgres:password@postgres:5432/routewise

# Mobile Development & API Configuration
# Default: Uses dynamic detection (localhost for local, IP for mobile)
# Override: Set to specific backend URL (e.g., http://192.168.1.100:4001)
VITE_API_URL=

# MSW (Mock Service Worker) Configuration
# Set to "true" to disable MSW and use real Phoenix backend
MSW_DISABLED=false

# Google API Keys - Get these from Google Cloud Console
# The app will work without these but with limited functionality
GOOGLE_PLACES_API_KEY=AIzaSyB8Teox86QyiwM8RMr32hvx-tnyHpE23AE
GOOGLE_MAPS_API_KEY=AIzaSyB8Teox86QyiwM8RMr32hvx-tnyHpE23AE

# Google OAuth Configuration for Client-side Authentication
# Get Google OAuth 2.0 Client ID from Google Cloud Console
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google Places API Cache Configuration (in minutes)
# Adjust these values based on your API usage and response freshness needs
GOOGLE_PLACES_CACHE_DURATION=5    # Places nearby search cache (default: 5 minutes)
GOOGLE_GEOCODING_CACHE_DURATION=10 # Geocoding results cache (default: 10 minutes)

# Redis Cache Configuration (optional - falls back to in-memory if not configured)
# For production, use Redis for shared caching across multiple instances
# REDIS_URL=redis://localhost:6379                    # Full Redis connection URL (preferred)
# REDIS_HOST=localhost                                # Redis host (alternative to URL)
# REDIS_PORT=6379                                     # Redis port (default: 6379)
# REDIS_PASSWORD=your_redis_password                  # Redis password (if required)
CACHE_DEFAULT_TTL=300000                              # Default cache TTL in milliseconds (5 minutes)
CACHE_KEY_PREFIX=routewise:                           # Cache key prefix to avoid conflicts
